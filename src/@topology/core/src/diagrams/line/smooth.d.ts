import { Point } from '../../point';
export declare function simplify(points: Point[], length: number, start: number, end: number): Point[];
export declare function smoothLine(points: Point[], cornerThres?: number, match?: boolean): any;
