import { TopologyStore } from '../store';
import { Pen } from './model';
export declare function renderFromArrow(ctx: CanvasRenderingContext2D, pen: Pen, store: TopologyStore): void;
export declare function renderToArrow(ctx: CanvasRenderingContext2D, pen: Pen, store: TopologyStore): void;
